import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project_factory_jacobi_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
